[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "coralflow"
version = "0.4.0"
description = "TinyML continuous training agent — dataset in, model out, deploy, monitor, auto-retrain"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Aifar"}]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "arize-phoenix-otel>=0.16",
    "click>=8.0",
    "google-cloud-aiplatform>=1.38",
    "psutil>=5.9",
    "python-dotenv>=1.0",
    "prompt-toolkit>=3.0",
    "requests>=2.28",
    "rich>=13.0",
    "tensorflow>=2.13",
    "aiohttp>=3.9",
]

[project.urls]
Homepage = "https://github.com/Aifar/coralflow"
Repository = "https://github.com/Aifar/coralflow"
Issues = "https://github.com/Aifar/coralflow/issues"

[project.scripts]
coralflow = "edge_train.cli:main"
coralflow-edge-pipeline = "edge_train.cli.edge_pipeline:main"

[project.optional-dependencies]
dev = [
    "black>=26.5",
    "flask>=3.0",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-mock>=3.14",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "slow: integration tests that train TensorFlow models",
]

[tool.setuptools.packages.find]
include = ["edge_train*"]
